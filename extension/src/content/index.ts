/**
 * TopicBlock content script.
 * Entry point for in-page segmentation, adapter dispatch, and filter actions.
 * WP-1: minimal stub — logs presence and wires up message receipt.
 */

console.log('[TopicBlock] Content script active on:', window.location.href);

// Placeholder: full pipeline wired in WP-4 (segmentation) + WP-9 (filter actions)
